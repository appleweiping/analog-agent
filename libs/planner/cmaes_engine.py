"""CMA-ES engine placeholder."""

from __future__ import annotations


def propose_cmaes_candidates(population: list[dict]) -> list[dict]:
    """Return the current population as a stub."""
    return list(population)
