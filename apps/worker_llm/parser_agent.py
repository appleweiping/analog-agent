"""Parser agent placeholder."""

from __future__ import annotations


class ParserAgent:
    """Parse human specifications into structured design intents."""

    def parse(self, prompt: str) -> dict:
        return {"agent": "parser", "status": "stub", "prompt": prompt}
