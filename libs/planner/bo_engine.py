"""Bayesian optimization engine placeholder."""

from __future__ import annotations


def propose_bo_candidates(observations: list[dict]) -> list[dict]:
    """Return observations unchanged until BO is implemented."""
    return list(observations)
